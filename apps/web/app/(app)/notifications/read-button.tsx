"use client";
import { useRouter } from "next/navigation";
import { useState } from "react";
export function MarkNotificationsRead({disabled}:{disabled:boolean}){const router=useRouter();const[busy,setBusy]=useState(false);const[error,setError]=useState("");async function mark(){setBusy(true);setError("");const response=await fetch("/api/v1/notifications",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({notificationId:null})});if(response.ok){window.dispatchEvent(new CustomEvent("campus:notifications-read"));router.refresh()}else setError("Notifications could not be updated. Please retry.");setBusy(false)}return <div className="notification-read-action"><button className="button button-ghost" disabled={disabled||busy} onClick={mark}>{busy?"Updating…":"Mark all read"}</button>{error&&<small className="form-error" role="alert">{error}</small>}</div>}
