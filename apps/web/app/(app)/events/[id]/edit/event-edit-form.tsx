"use client";import{useRouter}from"next/navigation";import{useState}from"react";type EventRow={id:string;title:string;description:string;location:string;starts_at:string;ends_at:string;capacity:number|null};
function localValue(value:string){const date=new Date(value);return new Date(date.getTime()-date.getTimezoneOffset()*60000).toISOString().slice(0,16)}
export function EventEditForm({event}:{event:EventRow}){const router=useRouter();const[busy,setBusy]=useState(false);const[error,setError]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const form=new FormData(e.currentTarget);const capacity=String(form.get("capacity")??"").trim();const response=await fetch(`/api/v1/events/${event.id}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({title:form.get("title"),description:form.get("description"),location:form.get("location"),startsAt:new Date(String(form.get("startsAt"))).toISOString(),endsAt:new Date(String(form.get("endsAt"))).toISOString(),capacity:capacity?Number(capacity):null})});const body=await response.json();if(response.ok){router.push("/my/events");router.refresh()}else{setError(body.error?.message??"Unable to update event.");setBusy(false)}}return <form className="listing-form" onSubmit={submit}><section><h2>Event details</h2><label>Title<input name="title" defaultValue={event.title} minLength={3} maxLength={120} required/></label><label>Description<textarea name="description" defaultValue={event.description} minLength={10} maxLength={5000} rows={5} required/></label><label>Location<input name="location" defaultValue={event.location} minLength={2} maxLength={200} required/></label><div className="form-row"><label>Starts<input name="startsAt" type="datetime-local" defaultValue={localValue(event.starts_at)} required/></label><label>Ends<input name="endsAt" type="datetime-local" defaultValue={localValue(event.ends_at)} required/></label></div><label>Capacity<input name="capacity" type="number" min="1" max="10000" defaultValue={event.capacity??""}/></label></section>{error&&<p className="form-error">{error}</p>}<button className="button button-primary" disabled={busy}>{busy?"Saving…":"Save changes"}</button></form>}
