"use client";
import Link from "next/link";
import { useRouter } from "next/navigation";
import { useState } from "react";
export function EventForm(){const router=useRouter();const[busy,setBusy]=useState(false);const[error,setError]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const f=new FormData(e.currentTarget);const capacity=String(f.get("capacity")??"").trim();const response=await fetch("/api/v1/events",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({title:f.get("title"),description:f.get("description"),location:f.get("location"),startsAt:new Date(String(f.get("startsAt"))).toISOString(),endsAt:new Date(String(f.get("endsAt"))).toISOString(),capacity:capacity?Number(capacity):null,idempotencyKey:crypto.randomUUID()})});const body=await response.json();if(!response.ok){setError(body.error?.message??"Unable to create event.");setBusy(false);return}router.push("/events");router.refresh()}return <form className="listing-form" onSubmit={submit}><section><h2>Event details</h2><label>Title<input name="title" minLength={3} maxLength={120} placeholder="e.g. Saturday pickup soccer" required/></label><label>Description<textarea name="description" minLength={10} maxLength={5000} rows={5} placeholder="What is happening, and who should come?" required/></label><label>Location<input name="location" minLength={2} maxLength={200} placeholder="Campus location" required/></label><div className="form-row"><label>Starts<input name="startsAt" type="datetime-local" required/></label><label>Ends<input name="endsAt" type="datetime-local" required/></label></div><label>Capacity (optional)<input name="capacity" type="number" min="1" max="10000"/></label></section>{error&&<p className="form-error" role="alert">{error}</p>}<div className="form-actions"><Link className="button button-ghost" href="/events">Cancel</Link><button className="button button-primary" disabled={busy}>{busy?"Publishing…":"Publish event"}</button></div></form>}
