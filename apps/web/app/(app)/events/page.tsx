import{CalendarDays,MapPin,Plus,Users}from"lucide-react";import Link from"next/link";import{loadEvents}from"@/lib/loaders";import{createSupabaseServerClient}from"@/lib/supabase/server";import{RsvpButton}from"./rsvp-button";
export const metadata={title:"Campus events"};
export default async function Events(){const events=await loadEvents();const db=await createSupabaseServerClient();const{data:{user}}=await db.auth.getUser();const{data:rsvps}=events.length?await db.from("event_rsvps").select("event_id").eq("profile_id",user!.id).in("event_id",events.map(event=>event.id)):{data:[]};const attending=new Set((rsvps??[]).map(item=>item.event_id));return <main className="dashboard"><section className="welcome-row"><div><span className="overline">CAMPUS CALENDAR</span><h1>Find your next thing.</h1><p>Real events created by verified members of your campus.</p></div><Link className="button button-primary" href="/events/new"><Plus/>Create event</Link></section>{events.length?<div className="event-grid">{events.map((event,index)=><article className={`event-card accent-${["coral","violet","green"][index%3]}`} key={event.id}><div className="event-date"><span>{new Date(event.starts_at).toLocaleString("en-US",{month:"short"}).toUpperCase()}</span><strong>{new Date(event.starts_at).getDate()}</strong></div><div className="event-body"><span>{new Date(event.starts_at).toLocaleString("en-US",{weekday:"long",hour:"numeric",minute:"2-digit"})}</span><h2>{event.title}</h2><p>{event.description}</p><div className="event-meta"><span><MapPin/>{event.location}</span><span><Users/>{event.event_rsvps?.[0]?.count??0} going</span></div></div><RsvpButton eventId={event.id} initialAttending={attending.has(event.id)}/></article>)}</div>:<div className="empty-state"><CalendarDays/><h2>No upcoming events</h2><p>Create the first event for your campus.</p><Link className="button button-primary" href="/events/new">Create event</Link></div>}</main>}
