"use client";
import { useState } from "react";import Link from "next/link";import {Search as SearchIcon} from "lucide-react";import {EmptyState,SurfaceCard} from "@/components/ui";
type Hit={kind:string;id:string;title:string;subtitle:string;href:string;campus_short_name:string;visibility:string};
export function SearchClient(){const[q,setQ]=useState("");const[types,setTypes]=useState<string[]>([]);const[rows,setRows]=useState<Hit[]>([]);const[searched,setSearched]=useState(false);const[notice,setNotice]=useState("");async function search(event:React.FormEvent){event.preventDefault();const params=new URLSearchParams({q,limit:"50"});types.forEach((type)=>params.append("type",type));const response=await fetch(`/api/v1/search?${params}`);const json=await response.json();setSearched(true);setRows(response.ok?json.data:[]);setNotice(response.ok?`${json.data.length} results`:json.error?.message??"Search unavailable.")}function toggle(type:string){setTypes((values)=>values.includes(type)?values.filter((value)=>value!==type):[...values,type])}return <><form className="global-search" onSubmit={search}><label><SearchIcon/><input value={q} onChange={(event)=>setQ(event.target.value)} minLength={2} maxLength={120} placeholder="Search Campus Exchange" required/></label><button className="button button-primary">Search</button><fieldset><legend>Filter result types</legend>{["profile","listing","organization","event","community","social_post"].map((type)=><label key={type}><input type="checkbox" checked={types.includes(type)} onChange={()=>toggle(type)}/>{type.replace("_"," ")}</label>)}</fieldset></form>{notice&&<p className="form-notice" role="status">{notice}</p>}<section className="search-results">{searched&&!rows.length&&<EmptyState icon={<SearchIcon/>} title="No visible results" description="Try a broader phrase or clear a result-type filter."/>}{rows.map((row)=><Link href={row.href} key={`${row.kind}-${row.id}`}><SurfaceCard className="search-result"><span className="ui-badge">{row.kind.replace("_"," ")}</span><div><h2>{row.title}</h2><p>{row.subtitle}</p></div><small>{row.campus_short_name} · {row.visibility.replace("_"," ")}</small></SurfaceCard></Link>)}</section></>}
