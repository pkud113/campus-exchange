import{createSupabaseServerClient}from"@/lib/supabase/server";import{ProfileSettings}from"../profile/profile-settings";
export const metadata={title:"Settings"};
export default async function Settings(){const db=await createSupabaseServerClient();const{data:{user}}=await db.auth.getUser();const[{data:profile},{data:roles}]=await Promise.all([db.from("profiles").select("handle,display_name,bio,verified_until,avatar_media_id,banner_media_id").eq("id",user!.id).single(),db.from("role_assignments").select("role").eq("profile_id",user!.id)]);return <main className="dashboard narrow"><div className="page-title"><span className="overline">YOUR ACCOUNT</span><h1>Profile & security</h1><p>Manage your public campus identity and account protection.</p></div><ProfileSettings profile={{username:profile?.handle??"",displayName:profile?.display_name??profile?.handle??"",bio:profile?.bio??"",verifiedUntil:profile?.verified_until??"",avatarId:profile?.avatar_media_id??null,bannerId:profile?.banner_media_id??null}} isStaff={Boolean(roles?.some(({role})=>role==="moderator"||role==="admin"))}/></main>}
