import Link from "next/link";
import { ArrowLeft, Heart, MapPin, MessageCircle, ShieldCheck } from "lucide-react";
import { demoListings } from "@/lib/demo";
type Props={params:Promise<{id:string}>};
export default async function ListingDetail({params}:Props){const{id}=await params;const item=demoListings.find(x=>x.id===id)??demoListings[0];return <main className="dashboard narrow"><Link className="back-link" href="/exchange"><ArrowLeft/>Back to marketplace</Link><div className="detail-layout"><div className={`detail-visual ${item.tone}`}><div className={`product-shape shape-${item.category}`}><i/><b/><em/></div></div><section className="detail-copy"><span className="condition-pill">{item.condition.replace("_"," ")}</span><h1>{item.title}</h1><strong className="detail-price">${(item.price_cents/100).toFixed(0)}</strong><p>{item.description}</p><div className="seller-card"><span className="avatar coral">{item.profiles.display_name[0]}</span><div><strong>{item.profiles.display_name}</strong><span><ShieldCheck/> Verified student</span><small><MapPin/> On campus</small></div></div><button className="button button-primary button-wide"><MessageCircle/> Message seller</button><button className="button button-ghost button-wide"><Heart/> Save listing</button><div className="safe-inline"><ShieldCheck/><p>Meet in a public campus location and inspect the item before paying.</p></div></section></div></main>}
