import Link from "next/link";
import { Plus, Search, ShieldCheck } from "lucide-react";
import { ListingCard } from "@/components/listing-card";
import { loadMarketplacePage } from "@/lib/loaders";

export const metadata={title:"Marketplace"};
export default async function Marketplace({searchParams}:{searchParams:Promise<{q?:string;category?:string;sort?:string;cursor?:string}>}){const filters=await searchParams;const{listings,nextCursor}=await loadMarketplacePage(filters);const nextParams=new URLSearchParams();if(filters.q)nextParams.set("q",filters.q);if(filters.category)nextParams.set("category",filters.category);if(filters.sort)nextParams.set("sort",filters.sort);if(nextCursor)nextParams.set("cursor",nextCursor);return <main className="dashboard"><section className="welcome-row"><div><span className="overline">MSU MARKETPLACE</span><h1>Find what you need.</h1><p>Browse listings shared by verified members of your campus.</p></div><Link className="button button-primary" href="/sell"><Plus size={18}/> Create listing</Link></section><form className="search-panel" action="/marketplace"><Search/><input name="q" defaultValue={filters.q} aria-label="Search marketplace" placeholder="Search desks, textbooks, bikes…"/><select name="category" defaultValue={filters.category??""} aria-label="Listing category"><option value="">All categories</option><option value="books">Books</option><option value="electronics">Electronics</option><option value="furniture">Furniture</option><option value="clothing">Clothing</option><option value="housing">Housing</option><option value="transport">Transport</option><option value="other">Other</option></select><select name="sort" defaultValue={filters.sort??"newest"} aria-label="Sort listings"><option value="newest">Newest</option><option value="price_asc">Price: low to high</option><option value="price_desc">Price: high to low</option></select><button type="submit">Search</button></form><section className="content-section"><div className="section-heading"><div><span className="overline">{filters.q||filters.category?"SEARCH RESULTS":"JUST LISTED"}</span><h2>{filters.q||filters.category?`${listings.length}${nextCursor?"+":""} matches`:"Fresh around campus"}</h2></div>{(filters.q||filters.category||filters.sort||filters.cursor)&&<Link href="/marketplace">Clear filters</Link>}</div>{listings.length?<><div className="listing-grid">{listings.map(listing=><ListingCard key={listing.id} listing={listing as never}/>)}</div>{nextCursor&&<div className="pagination-actions"><Link className="button button-ghost" href={`/marketplace?${nextParams.toString()}`}>Load more listings</Link></div>}</>:<div className="empty-state"><Search/><h2>No listings found</h2><p>Try another search or create the first matching listing.</p></div>}</section><section className="safe-card"><ShieldCheck/><div><strong>Trade smart. Meet safely.</strong><p>Meet in a busy campus location, inspect items first, and never send deposits to hold a listing.</p></div><Link href="/safety">Safety tips</Link></section></main>}
