"use client";
import { useRouter } from "next/navigation";
import Link from "next/link";
import { useState } from "react";
import { ImagePlus, LoaderCircle } from "lucide-react";
export function ListingForm(){const router=useRouter();const[busy,setBusy]=useState(false);const[error,setError]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const f=new FormData(e.currentTarget);const response=await fetch("/api/v1/listings",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({title:f.get("title"),description:f.get("description"),category:f.get("category"),condition:f.get("condition"),priceCents:Math.round(Number(f.get("price"))*100),currency:"USD",idempotencyKey:crypto.randomUUID()})});const body=await response.json();if(!response.ok){setError(body.error?.message??"Could not publish this listing.");setBusy(false);return}const files=f.getAll("photos").filter((value):value is File=>value instanceof File&&value.size>0).slice(0,6);for(const file of files){const grant=await fetch("/api/v1/uploads",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({listingId:body.data.id,contentType:file.type,byteSize:file.size,altText:String(f.get("title"))})});if(!grant.ok)continue;const upload=await grant.json();await fetch(upload.data.uploadUrl,{method:"PUT",headers:{"content-type":file.type,"content-length":String(file.size)},body:file}).catch(()=>undefined)}router.push(`/listings/${body.data.id}`)}return <form className="listing-form" onSubmit={submit}><section><h2>Photos</h2><label className="photo-drop"><ImagePlus/><strong>Add up to 6 photos</strong><span>WebP, PNG, or JPEG · 8 MB each</span><input name="photos" type="file" accept="image/webp,image/png,image/jpeg" multiple aria-label="Listing photos"/></label></section><section><h2>Details</h2><label>Title<input name="title" minLength={3} maxLength={100} placeholder="What are you selling?" required/></label><div className="form-row"><label>Category<select name="category" defaultValue=""><option value="" disabled>Choose one</option><option value="books">Books</option><option value="electronics">Electronics</option><option value="furniture">Furniture</option><option value="clothing">Clothing</option><option value="housing">Housing</option><option value="transport">Transport</option><option value="other">Other</option></select></label><label>Condition<select name="condition" defaultValue="good"><option value="new">New</option><option value="like_new">Like new</option><option value="good">Good</option><option value="fair">Fair</option><option value="poor">Well used</option></select></label></div><label>Description<textarea name="description" minLength={10} maxLength={5000} rows={6} placeholder="Share dimensions, included parts, wear, and pickup details…" required/></label><label>Price<div className="price-input"><span>$</span><input name="price" type="number" min="0" max="100000" step="0.01" placeholder="0.00" required/></div></label></section>{error&&<p className="form-error" role="alert">{error}</p>}<div className="form-actions"><Link className="button button-ghost" href="/exchange">Cancel</Link><button className="button button-primary" disabled={busy}>{busy?<><LoaderCircle className="spin"/>Publishing…</>:"Publish listing"}</button></div></form>}
