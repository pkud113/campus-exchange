import Link from"next/link";import{Plus,ShoppingBag}from"lucide-react";import{ListingCard}from"@/components/listing-card";import{OwnerContentActions}from"@/components/owner-content-actions";import{createSupabaseServerClient}from"@/lib/supabase/server";
export default async function MyListings(){const db=await createSupabaseServerClient();const{data:{user}}=await db.auth.getUser();const{data}=await db.from("listings").select("id,title,category,condition,price_cents,currency,status,profiles!listings_seller_id_fkey(handle,display_name),media_uploads(id,alt_text,status)").eq("seller_id",user!.id).is("deleted_at",null).order("created_at",{ascending:false});return <main className="dashboard"><div className="page-title page-title-actions"><div><span className="overline">YOUR CONTENT</span><h1>My Listings</h1></div><Link className="button button-primary" href="/sell"><Plus/>Create listing</Link></div>{data?.length?<div className="managed-grid">{data.map(item=><article key={item.id}><ListingCard listing={item as never}/><OwnerContentActions type="listing" id={item.id}/></article>)}</div>:<div className="empty-state"><ShoppingBag/><h2>No listings yet</h2><Link href="/sell">Create your first listing</Link></div>}</main>}
