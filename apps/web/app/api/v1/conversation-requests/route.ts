import{conversationRequestInputSchema}from"@campus-exchange/contracts";import{apiData,apiError,enforceRateLimit,parseJson,requireVerified,verifyMutationOrigin}from"@/lib/api";import{NextResponse}from"next/server";
export async function GET(request:Request){const c=await requireVerified(request);if(c instanceof NextResponse)return c;const{data,error}=await c.supabase.from("conversation_requests").select("id,requester_id,recipient_id,status,created_at,responded_at,requester:profiles!conversation_requests_requester_id_fkey(handle,display_name,avatar_media_id),recipient:profiles!conversation_requests_recipient_id_fkey(handle,display_name,avatar_media_id)").in("status",["pending"]).order("created_at",{ascending:false});return error?apiError(request,500,"internal_error","Unable to load conversation requests."):apiData(request,data??[])}
export async function POST(request:Request){const e=verifyMutationOrigin(request);if(e)return e;const c=await requireVerified(request);if(c instanceof NextResponse)return c;const limited=await enforceRateLimit(request,"conversation-request",c.userId,20,3600);if(limited)return limited;const input=await parseJson(request,conversationRequestInputSchema);if(input instanceof NextResponse)return input;const{data,error}=await c.supabase.rpc("create_direct_conversation_request",{target_profile:input.profileId});if(error?.code==="23505")return apiError(request,409,"conflict","A pending request or conversation already exists.");return error?apiError(request,409,"conflict",error.message):apiData(request,{id:data},201)}
