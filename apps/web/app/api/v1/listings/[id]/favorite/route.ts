import { apiData, apiError, requireVerified, verifyMutationOrigin } from "@/lib/api";
import { NextResponse } from "next/server";
type Params = { params: Promise<{ id: string }> };
export async function POST(request: Request, { params }: Params) { const e=verifyMutationOrigin(request); if(e)return e; const c=await requireVerified(request); if(c instanceof NextResponse)return c; const {id}=await params; const {error}=await c.supabase.rpc("set_listing_favorite",{target_listing:id,desired:true}); return error?apiError(request,400,"bad_request","Unable to save this listing."):apiData(request,{favorite:true}); }
export async function DELETE(request: Request, { params }: Params) { const e=verifyMutationOrigin(request); if(e)return e; const c=await requireVerified(request); if(c instanceof NextResponse)return c; const {id}=await params; const {error}=await c.supabase.rpc("set_listing_favorite",{target_listing:id,desired:false}); return error?apiError(request,400,"bad_request","Unable to remove this listing."):apiData(request,{favorite:false}); }
