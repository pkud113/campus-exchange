import{z}from"zod";import{apiData,apiError,parseJson,requireStaff,verifyMutationOrigin}from"@/lib/api";import{NextResponse}from"next/server";type Params={params:Promise<{id:string}>};
const schema=z.object({action:z.enum(["suspend","restore"]),reason:z.string().trim().min(3).max(1000)});
export async function PATCH(request:Request,{params}:Params){const originError=verifyMutationOrigin(request);if(originError)return originError;const c=await requireStaff(request);if(c instanceof NextResponse)return c;const input=await parseJson(request,schema);if(input instanceof NextResponse)return input;const{id}=await params;const{error}=await c.supabase.rpc("moderate_profile",{target_profile:id,chosen_action:input.action,action_reason:input.reason});return error?apiError(request,403,"forbidden","Unable to change this account."):apiData(request,{updated:true,status:input.action==="suspend"?"suspended":"active"})}
