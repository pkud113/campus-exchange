"use client";import{Monitor,Moon,Sun}from"lucide-react";import{useEffect,useState}from"react";type Theme="system"|"light"|"dark";
function apply(theme:Theme){const resolved=theme==="system"?(matchMedia("(prefers-color-scheme: dark)").matches?"dark":"light"):theme;document.documentElement.dataset.theme=resolved;document.documentElement.style.colorScheme=resolved;localStorage.setItem("campus-theme",theme);document.cookie=`campus-theme=${theme}; Path=/; Max-Age=31536000; SameSite=Lax; Secure`}
export function ThemeToggle(){const[theme,setTheme]=useState<Theme>("system");useEffect(()=>{const saved=localStorage.getItem("campus-theme")as Theme|null;if(saved)setTheme(saved)},[]);const next:Theme=theme==="system"?"light":theme==="light"?"dark":"system";const Icon=theme==="system"?Monitor:theme==="light"?Sun:Moon;return <button className="nav-item nav-button" onClick={()=>{setTheme(next);apply(next)}} title={`Theme: ${theme}. Switch to ${next}.`}><Icon/>Theme: {theme}</button>}
