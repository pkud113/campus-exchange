{
  "name": "@campus-exchange/domain",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@campus-exchange/contracts": "workspace:*",
    "@campus-exchange/shared-types": "workspace:*"
  },
  "devDependencies": { "typescript": "^7.0.2", "vitest": "^4.1.10" }
}
